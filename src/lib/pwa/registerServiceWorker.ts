export function registerServiceWorker() {
  // The production service worker is registered by vite-plugin-pwa.
  // Keeping this no-op avoids making the initial app load depend on SW registration.
}
